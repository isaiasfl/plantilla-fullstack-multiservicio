# Archivo de ejemplo para variables de entorno
# Copia este archivo a .env y ajusta los valores según tu configuración

# Configuración del servidor
NODE_ENV=development
PORT=3000

# URL del frontend para CORS
FRONTEND_URL=http://localhost:5173

# Configuración de MongoDB (descomenta si lo usas)
# MONGODB_URI=mongodb://admin:admin123@mongo:27017/plantilla_db?authSource=admin

# Configuración de PostgreSQL (descomenta si lo usas)
# POSTGRES_URL=postgresql://user:password@postgres:5432/plantilla_db
# DATABASE_HOST=postgres
# DATABASE_PORT=5432
# DATABASE_NAME=plantilla_db
# DATABASE_USER=user
# DATABASE_PASSWORD=password

# Configuración de SQLite (descomenta si lo usas)
# DATABASE_URL=file:./dev.db

# Secretos de la aplicación (genera claves seguras en producción)
# JWT_SECRET=tu-jwt-secret-muy-seguro-aqui
# API_KEY=tu-api-key-aqui

# Configuración de logs
LOG_LEVEL=info

# Configuración de CORS (URLs permitidas separadas por comas)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000